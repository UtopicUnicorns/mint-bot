T2: 
while true; do neofetch --stdout > neofetch.txt; sleep 60; done

BASHRC:
#shutdown -r +60 "test"
cd /home/libro/Downloads/
mv config.json /home/libro/Downloads
rm clone.zip
zip -r clone.zip ./
mv /home/libro/Downloads/config.json /home/libro/Downloads/
neofetch --stdout > neofetch.txt
#node index.js

T1:
sudo cpufreq-set -c 0 -u 999MHz;sudo cpufreq-set -c 1 -u 999MHz;sudo cpufreq-set -c 2 -u 999MHz;sudo cpufreq-set -c 3 -u 999MHz;


while true; do node index.js; done

